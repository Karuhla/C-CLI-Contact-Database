# CLI Contact Database (C)

A simple command-line contact database written in C.

The program allows users to store and manage contacts through a terminal interface.

## Features

- Insert contacts
- View stored contacts
- Dynamic memory management using `malloc` and `realloc`
- Command-line interface
- Struct-based data storage